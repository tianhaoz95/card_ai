your task is to consolidate the prompt used in `getBestCardMatch` in `lib/services/cactus_llm_service.dart` into a prompt template for better readability.

your task is to rename the user visiable app name from card_ai to "Card.ai"